# 🚀 8주 완성 알고리즘 코딩테스트 스터디

> **"백준 골드 수준의 문제를 AI 도움 없이 스스로 해결하는 역량 기르기"**
>
> 본 스터디는 웹 개발 역량의 기초가 되는 문제 해결 능력을 기르기 위해 기획되었습니다.

---

## 👥 스터디 멤버
- **Member A (나):** 프론트/백엔드 공부 중 (C++ 포인터, 클래스 숙련)
- **Member B (선배):** 대학 선배 (C++ 포인터 숙련, 자료구조 수강 중)

## 🎯 목표
1. **No AI Policy:** 문제 풀이 시 생성형 AI의 도움을 받지 않고 자력으로 구현한다.
2. **C++ STL 마스터:** `vector`, `sort`, `stack`, `queue` 등 필수 라이브러리 숙달.
3. **학교 성적 시너지:** C언어 자료구조 수업과 병행하여 이론과 실무 능력을 동시에 확보한다.

---

## 📅 8주 커리큘럼 로드맵

| 주차 | 주제 | 핵심 개념 (Prerequisite) | 필수 문제 (BOJ) |
| :--- | :--- | :--- | :--- |
| **1주** | **C++ 기초 & 시간 복잡도** | Fast I/O, `vector`, $O(N)$ 개념 | 2557, 15552, 10818, 2750 |
| **2주** | **기초 자료구조** | 스택, 큐, 덱 (원리 이해) | 10828, 10845, 10866 |
| **3주** | **완전 탐색 (Brute Force)** | 재귀(Recursion), 기저 조건 설정 | 2798, 2231, 1018 |
| **4주** | **그래프 탐색 (BFS/DFS)** | 인접 리스트 구현, 방문 배열 처리 | 1260, 2606, 2178 |
| **5주** | **이분 탐색** | Binary Search, 매개 변수 탐색 | 1920, 2805, 1654 |
| **6주** | **그리디 (Greedy)** | 정렬과 최적 선택, 증명 | 11047, 1931, 1541 |
| **7주** | **다이나믹 프로그래밍** | 점화식 설계, Memoization | 1463, 9095, 2579 |
| **8주** | **최단 경로 (Dijkstra)** | 우선순위 큐 기반 최단거리 알고리즘 | 1753, 1916, 1504 |

---

## ⏰ 데일리 루틴 (120분)

1. **[00:00 ~ 00:40] 개념 정복**
   - 주차별 키워드 이론 학습 (블로그/유튜브 활용)
   - 학교 자료구조(C언어) 수업 내용과 비교 분석
2. **[00:40 ~ 01:40] 자력 문제 풀이**
   - 백준 사이트에서 필수 문제 구현
   - **Tip:** 30분 이상 막히면 힌트만 보고 다시 시도
3. **[01:40 ~ 02:00] 코드 리뷰**
   - 서로의 코드 공유 및 시간 복잡도 개선점 토론

---

## 🛠 C++ 필수 템플릿 (Fast I/O)
C++로 문제를 풀 때는 입출력 속도로 인한 시간 초과를 방지하기 위해 아래 설정을 반드시 포함합니다.

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    // 입출력 가속 설정
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // 알고리즘 작성
    
    return 0;
}
```

---

## 📁 저장소 구조 (실제 구현 코드)

주차별 폴더에 백준(BOJ) 문제 풀이 코드(`.cpp`)가 정리되어 있습니다. 아래는 각 폴더에 실제로 담긴 주제와 대표 문제입니다.

> 참고: 실제 구현 순서상 **그리디와 DP 주차 순서**는 위 로드맵 계획과 반대로 진행되어, `week6`에 DP · `week7`에 그리디가 들어 있습니다.

| 폴더 | 주제 | 대표 파일 (문제) |
| :--- | :--- | :--- |
| `week1/` | **정렬 & 시간 복잡도** | `timeComplex`(15552 Fast I/O), `sortWithVector`(2750), `minMax`(10818), `ageSort`(10814), `coorSort`(11650) |
| `week2/` | **기초 자료구조 (Stack·Queue)** | `stack`, `queue`, `stackseq`(스택 수열), `yosefus`(요세푸스) |
| `week3/` | **완전 탐색 & 백트래킹** | `blackjack`(2798, 순열/조합/3중 반복 3가지 구현 비교), `lotto`, `nm`(N과 M), `nqueen`(N-Queen) |
| `week4/` | **그래프 탐색 (BFS·DFS)** | `dfsbfs`(1260), `mindist`(미로 탐색), `tomato`(토마토), `cabbage`(유기농 배추), `worm_bfs`·`worm_dfs`(바이러스) |
| `week5/` | **이분 탐색 & 매개변수 탐색** | `binary_Search`(1920), `wood`(나무 자르기), `lan`(랜선 자르기), `router`(공유기 설치) |
| `week6/` | **다이나믹 프로그래밍 (DP)** | `fibo`(피보나치), `make1`(1로 만들기), `123sum`(1·2·3 더하기), `2ntile`(2×n 타일링), `rgb`(RGB거리), `baggage`(배낭/Knapsack) |
| `week7/` | **그리디 & 힙** | `coin_zero`(동전 0), `meeting`(회의실 배정), `cards`(카드 정렬하기·min-heap), `maxheap`(최대 힙) |
| `week8/` | **최단 경로 (Dijkstra·Floyd)** | `mindist`(1753), `mindist_bus`(1916), `mindist_spec`(1504 특정 최단경로), `floyd`(플로이드-워셜) |

각 코드는 스터디의 **No AI Policy** 원칙에 따라 직접 구현되었으며, 완전 탐색 주차의 블랙잭처럼 동일 문제를 순열/조합/반복문 등 여러 방식으로 구현해 시간 복잡도 차이를 비교한 사례도 포함되어 있습니다.

---

## 👤 기여도 & 개발 환경

| 항목 | 내용 |
|---|---|
| **기여 비율** | **100%** (단독 개발) |
| **커밋** | 13 / 13 (본인 / 전체 사람 커밋) |
| **참여 인원** | 1명 |

<sub>기여 비율은 커밋 author 이메일 기준 집계이며 봇·자동화 커밋은 제외했습니다.</sub>
